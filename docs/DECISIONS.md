# Decision log

One entry per decision. Newest at the bottom. Add an entry whenever a choice is made that a future reader would otherwise have to guess.

| # | Decision | Reason |
|---|---|---|
| 1 | Build our own model instead of calling the Jev API | The goal is learning the training side, in particular RLCD, and an interview-ready story; an API demo would not deliver either. |
| 2 | Post-training with LoRA on a pretrained base, not pretraining from scratch | A from-scratch model on a T4 budget cannot learn enough language to read option descriptions. |
| 3 | Main backbone Qwen3-1.7B-Base; Qwen3-0.6B-Base for pipeline debugging and RLCD sweeps; 4B only as an optional QLoRA experiment | 1.7B reads option descriptions far better than 0.6B while staying clearly smaller than the 4B generator in the target agent; 1.7B still fits fp32 on a T4 if fp16 overflows, 4B does not; 0.6B makes multi-seed ablations cheap; two sizes give a scaling row in the report for free. |
| 4 | Readout via language-model logits at answer slots restricted to option letters, no custom head | Zero new parameters, one forward pass, multi-question in one sequence, and the frozen base is an immediate baseline. Per-candidate sequences (NanoJev style) are too slow to serve; a marker head is kept as a fallback if letter bias or the 26-option cap bites. |
| 5 | Options are always written into the prompt and may vary per request | This is what separates a System One model from a fine-tuned classifier; it is also what makes the unseen-intent and unseen-schema tests meaningful. |
| 6 | v1 data from public datasets (CLINC150, SST-5); LLM API used only to write option descriptions and later to synthesise rare branches; LLM also serves as a baseline | Real human text and trusted labels for a credible test set; synthetic-only evaluation would be circular. |
| 7 | Hold out 20 intents and three whole datasets that never appear in training | Evidence that the model reads options rather than memorising labels. |
| 8 | Confidence for choice and score is 1 - H(p)/ln(K) | TypeSafe has not published its formula; normalised entropy is simple, bounded in [0, 1], and recoverable from the full distribution we already return. |
| 9 | RLCD is defined operationally as policy gradient under bandit feedback with a proper scoring rule as reward, group-mean baseline and a KL term to the SFT policy | TypeSafe has published the objective but not the method; this is the reconstruction used by the public replications (Laya, eve-rlcd) and it maps onto the author's GRPO experience. |
| 10 | RLCD must be compared against SFT plus temperature scaling, and the primary claim is calibration on unseen schemas | With full labels, cross-entropy already optimises a proper score, so in-domain gains are not expected; the honest test is transfer of calibration and behaviour under partial feedback. |
| 11 | Integration host is the delta-filing agent (SEC filings, 12 MCP tools, review loop), not the insurance ReAct agent | delta-filing has many decision points of different kinds per query, so the cascade story is measurable; the insurance loop is nearly deterministic and would be better served by hardcoding. |
| 12 | The engine is built standalone first (phases 1 and 2) and integrated later (phase 3) | The interface is the contract; integration is one wrapper class once the engine exists. |
| 13 | v1 caps choice at 26 options | One letter per option keeps the readout trivial; larger sets are a v2.4 concern via a marker head or two-stage selection. |
| 14 | Project name jevmark; package jevmark; public function stays systemone | A quick search found no existing project with the name. The mark part is read as a measured reproduction: the differentiator of this project is honest evaluation (unseen schemas, RLCD ablations against temperature scaling), so the name fits the product. |
| 15 | fp16 autocast with automatic fp32 fallback on NaN or inf | T4 has no bf16; Qwen models are trained in bf16 and can overflow in fp16. The check is cheap and removes a whole class of silent failures. |
| 16 | LLM API is a mini-tier OpenAI model (GPT-4o-mini or its current successor) for descriptions and the cost baseline; the current flagship on a 200-example subset as the accuracy ceiling | Expected spend is under a dollar for descriptions and a few dollars for baselines; the caps in TASKS.md are guardrails against runaway loops, not budgets. |
| 17 | Encoding tokenizes per segment (state block, then separator plus question block for each question) and concatenates ids | Qwen's pre-tokenizer can merge `:` with following newlines when the whole text is tokenized at once, which would move the answer slot. Per-segment tokenization pins `Answer:` to a token boundary; training and inference share the one function. Raised by Claude Code during the doc review. |
| 18 | Noul has no confidence field in the response; for metrics only, noul confidence is max(p, 1 - p) | Keeps the Jev response contract while giving the coverage curve a defined rule for every question type. ECE uses top-1 probability, coverage uses the confidence field; both are stated in every report. |
| 19 | Option order in every stored split is a seeded random order fixed at build time; the API preserves caller order | Without this, gold letters cluster in test splits and letter bias becomes unmeasurable. |
| 20 | Repo hygiene: `/data/` and adapters ignored, metrics and configs committed; uv with Python 3.11; commits on main per task; tokenizer fetched from the Hub at a pinned revision, not vendored | Reports must link to committed metrics; a bare `data/` pattern would hide `jevmark/data/`; solo project needs no branches. |
| 21 | Build backend hatchling; on Linux, uv pulls torch from the PyTorch CPU index; macOS uses PyPI wheels, which are already CPU builds | Local development is CPU only and a Linux sync should never download CUDA wheels; Kaggle keeps its preinstalled torch. |
| 22 | Tiny test model: Qwen3Config with 2 layers, hidden 64, 4 heads (2 KV), head_dim 16, tied embeddings, vocab len(tokenizer); tokenizer from Qwen/Qwen3-0.6B-Base at revision da87bfb6 | The two backbones share one tokenizer; tied embeddings keep the 151k-row vocabulary to one matrix so tests stay fast on CPU. |
| 23 | On Kaggle, keep the preinstalled torch and numpy (with the pandas, pyarrow and scipy built against it) and pip install only transformers, tokenizers, peft, datasets, accelerate, huggingface_hub and safetensors at the exact versions in uv.lock, with no transitive pins; applies from task 1.6 | The Hugging Face stack, which decides tokenization, model loading and the LoRA code, then matches the local environment exactly, while torch and numpy stay the builds the image's other libraries (pandas, pyarrow, scipy) were compiled against; pinning numpy risked breaking those. |
| 24 | Schema validation beyond the literal spec: unknown question fields are rejected; at least one question is required; instructions and option labels must be non-empty after stripping; duplicate choice labels are compared after stripping whitespace; invalid question ids are reported under the path `questions`; a JSON state must be strictly JSON-serialisable (no NaN) and its 8000-character limit applies to the rendered text | A typo such as `criterion` would otherwise be ignored silently; a JSON object cannot repeat a key, so whitespace variants are the only way two option lines can collide after parsing; the limit guards what the model reads, which is the rendered text. `serve.py` (task 1.4) must parse request bodies with a duplicate-key check, because `json.loads` keeps only the last duplicate. |
| 25 | Instructions, choice labels, choice descriptions, score level texts and noul descriptions must be single lines with no leading or trailing whitespace; the state may contain newlines | The encoding puts each of these on its own line, so a line break would forge an extra option or question line and edge whitespace would render as an invisible difference. Supersedes the whitespace-stripped duplicate comparison in decision 24: labels with edge whitespace are now rejected, so duplicates are compared exactly. Noul descriptions were added to the human's list of fields because they are rendered on option lines in the same way. Empty-string descriptions are rejected too: a description is null where null is allowed, or a non-empty string, so no option line ends in a dangling `: `. |
| 26 | Encoding details: the reference tokenizer is pinned once in `configs/base.yaml` (`tokenizer_reference`) and read by both `encode.load_tokenizer` and the tests; tokenizer assumption failures (letter tokens, parity, slot not ending in `:`) raise `TokenizerError`, a `RuntimeError`, while an over-long request raises `ValueError("max_tokens: ...")`; `encode` recomputes the 26 letter ids on every call instead of caching them | One pin avoids the test tokenizer and the parity reference drifting apart; a broken tokenizer is a setup fault, not bad input, so it must not be caught as a validation error; the encoded length belongs to no single question, so its path is the argument name; no cache means no global mutable state, at a cost of 26 short tokenizer calls per request. |
| 27 | Accept that per-segment encoding shows the frozen base (B0) an unfamiliar sequence, and state it in the v1 report | Per-segment encoding makes `:` and `\n\n` two tokens where natural text usually has the single token `:\n\n`, so B0 sees a slightly unfamiliar sequence at every slot but the last. This is a known small handicap for B0 and irrelevant after LoRA. |
| 28 | Precision (revised in task 1.6b): on CUDA with `precision.autocast: fp16`, `JevMark.load` loads the frozen backbone in fp16 and runs fp16 autocast, keeps LoRA parameters in fp32, and computes the letter readout in fp32; on CPU every weight is fp32. `use_fp32()` is the hook the first-batch NaN or inf check in the scripts (tasks 1.6, 1.7) calls, and it reloads the model in fp32 from the same backbone and checkpoint; a model built without `load` is cast to fp32 in place. A missing `calibration.json` means temperature 1.0 and a missing `model_id.txt` means `jevmark-<run_name>`, but a checkpoint without `adapter/` is a `RuntimeError`; `load` sets eval mode and otherwise train or eval mode is the caller's choice | The first version kept fp32 weights under fp16 autocast, so every matmul recast its weights to fp16 on every forward pass. Measured on 0.6B on a T4 (task 1.7): this explained about 20 percent of batch-1 latency (59.7 ms with fp32 weights, 47.5 ms with fp16); the rest is kernel launch and Python overhead at batch 1, which fp16 weights cannot remove. An unmerged LoRA adapter adds about 45 percent on top (69.2 ms for sft_06b), so evaluate.py merges the adapter into the backbone for inference by default (`JevMark.merge_lora`, `--no-merge` to keep it separate). fp16 frozen weights also halve backbone memory for 1.7B training. LoRA stays fp32 so its updates are not lost to fp16 rounding, and the readout stays fp32 so probabilities are not. Reloading rather than casting up recovers the checkpoint's original bf16 values instead of their fp16 rounding. The spec gives defaults for calibration but an adapter-less checkpoint is a broken run; training switches modes around validation, so the model must not switch them silently. |
| 29 | systemone details: the default model is an `lru_cache(maxsize=1)` on `default_model()`, the one process-wide cache in the package; with `JEVMARK_CHECKPOINT` set, the run's own `config.yaml` is preferred over `configs/base.yaml`; answers are computed in float64 after renormalising the distribution, and a rounded `-0.0` is written as `0.0`; `systemone_batch` takes `batch_size` (default 16) and validates and encodes every request before any forward pass; `serve.py` reports body errors (invalid JSON, duplicate key) under the path `request`; the dev extras include `httpx` for `fastapi.testclient` | "Load once per process" needs a cache, and one explicit cached function is easier to reset in tests than a module global; a run's adapter must meet the backbone it was trained on, which base.yaml may not name; float64 keeps argmax, expectation and entropy free of fp32 noise; batching without a cap would put an arbitrarily long list into one forward pass; body errors belong to no question, and `request` is already the path for a request that is not an object. |
| 30 | Batching contract: batching has no semantic effect, and numerical differences between batched and single calls are bounded by the arithmetic precision of the run and measured in evaluation; the CPU test compares every float within 1e-4 | Batched and single forward passes are not bitwise equal, so exact equality of 4-decimal responses cannot be guaranteed; a measured bound in metrics.json is an honest claim. Measured values (updated in task 1.6b): letter logits differed by about 1.5e-8 on CPU in task 1.4; probabilities differed by at most 6e-8 on CPU with the tiny test model (task 1.6 smoke run), and by 0.0041 (0.6B) and 0.0065 (1.7B) under fp16 on a T4 in the first B0 run, which still had fp32 weights under autocast. Measured again in task 1.7 with fp16 weights on data v1.3: 0.0068 to 0.0094 across both sizes (0.0068 `base_06b`, 0.0089 `sft_06b`, 0.0094 `base_17b`, 0.0090 `sft_17b`). So the contract is about 1e-4 in fp32 and up to about 1e-2 under fp16 on GPU. |
| 31 | Dataset ids and description generation: `clinc_oos` is loaded as `clinc/clinc_oos` and `PolyAI/banking77` as its parquet mirror `legacy-datasets/banking77`, both pinned by commit in `jevmark/data/sources.py`, the one place loaders read ids from; descriptions are generated with one API call per label whose prompt lists every label of the dataset for contrast, and the spend cap is computed from reported token usage and prices given on the command line | datasets 5 needs namespaced ids and no longer runs loading scripts; the Banking77 mirror is identical row by row to the original PolyAI CSVs (docs/DATA.md section 3), so the data is unchanged; listing all labels lets each description separate its label from near neighbours; prices change, so the script cannot know them and a cap without real prices would be a fake guardrail. |
| 32 | CLINC noul balance: every out-of-scope utterance yields two records, one `out_of_scope` (true) and one `about_domain` (false); an in-scope utterance gets `out_of_scope` (false) with p = out-of-scope count / in-scope count for its split, else `about_domain`, whose asked domain is the gold one with q = N_in / (2 (N_in - N_oos)) so the extra out-of-scope `false` answers are cancelled; `build_data.py` fails if any noul kind in any split has a yes or no share outside 40 to 60 percent | The earlier rule gave about 125 true against 1200 false for `out_of_scope` in train, because train has only 250 out-of-scope utterances; a noul trained on that would learn the base rate, not the question. With q fixed at 0.5, `about_domain` in `test_indomain` would be 37 percent yes (1000 out-of-scope `false` records against about 2900 in-scope ones), so q is derived per split. The fail check makes a future imbalance impossible to miss. |
| 33 | Generated descriptions are committed as raw output and never edited; corrections go in `overrides.json` with a reason per label, and one loader applies overrides and then a fixed normalisation (curly apostrophe to straight, one trailing period stripped) to every text | The raw files stay a faithful record of what the model produced, every human correction is visible with its reason, and a single loader means no builder can read uncorrected or unnormalised text by accident. |
| 34 | Data build details: one generator per split from `random.Random(f"{seed}:{split}")`; option order uniformity is checked conditional on K (no position more than 4 binomial standard deviations from 1/K, K groups under 100 skipped) instead of on the unconditional letter histogram; `train` and `valid` files hold CLINC records followed by SST-5 records, left for the loader to shuffle; `other` has a fixed description (`None of the listed intents` for CLINC, `None of the listed options` for Banking77); SST-5 levels are checked against the dataset's `label_text` so a relabelled revision fails | String seeds make each split independent of the others; the unconditional histogram cannot be uniform when K varies and noul questions have two options, so it would be a false check; a fixed `other` text keeps that option identical everywhere; checking `label_text` guards the one place where the dataset's integer labels are trusted. |
| 35 | Evaluation details: metrics come from unrounded `forward_distributions`; macro-F1 averages over labels that occur as gold (other predictions still count as false positives); coverage is reported per question type only, since its confidence differs by type; noul symmetry reports `argmax_consistent`, the share of pairs where the answer to q is yes exactly when the answer to not q is no; a `--limit` run writes to `runs/<run_name>_limit<N>/` (gitignored); every `metrics.json` records the commit, a dirty flag (taken before any output is written and ignoring `runs/`, since a re-run overwrites the tracked files of the run it replaces) and the sha256 of each data file read; `requirements-kaggle.txt` is generated from `uv export --frozen` (its package list is set by decision 23); the Kaggle notebook passes the GitHub token to git through environment variables only; the Makefile takes `PY` (uv locally, python on Kaggle) and `make eval` passes `--config` only when `EVAL_CONFIG` is set | Rounded responses would hide calibration; labels never gold would dilute macro-F1 with undefined classes; pooling two confidence definitions in one curve would be meaningless; "agree" in the plain sense would count contradictions as agreement; smoke runs must never overwrite results and every number must trace to code and data; tokenizers decides the ids, so it is pinned with the Hugging Face packages; a token on a command line or in `.git/config` can leak into logs or outputs; the old `make eval` would have overridden a run's own config with base.yaml. |
| 36 | Letter ids are cached per tokenizer object in a module-level `weakref.WeakKeyDictionary` in `encode.py`, computed and checked on first use; this supersedes the per-call recomputation in decision 26 | Recomputing 26 tokenizer calls per request was measurable overhead in the B0 latency; weak keys tie each entry to the tokenizer it came from, so a different or rebuilt tokenizer is always checked again and nothing outlives its tokenizer. This is the package's second process-wide cache, after `default_model()`; tokenizers here are never mutated after loading. |
| 37 | `evaluate.py` writes `runs/<run_name>/results.jsonl.gz`, one line per question (record id, question id, split, type, kind, labels, probs, gold, confidence, prediction, and P(yes) for the negated noul instruction); `scripts/recompute_metrics.py` rebuilds the per-split metrics from it with the same function evaluate uses and keeps run-level fields from the existing `metrics.json`; the file is gitignored. Size: 29.7 KiB for the tiny `--limit 30` smoke run (360 questions), 4.4 MiB for all eight splits (65255 questions) | New metrics can then be added to old runs without a GPU session, and every reported number has per-question evidence behind it; the file stays out of git because it is regenerable from a pinned commit and its size grows with every run. |
| 38 | B0 is re-evaluated in the task 1.7 Kaggle session with the extended `evaluate.py` (fp16 frozen weights, letter id cache, results file, new breakdowns); the committed `runs/base_06b` and `runs/base_17b` from commit 79fc74b stay as the first measurement and are replaced by the re-run | Every number in the v1 report must come from one code version and have a `results.jsonl.gz` behind it; the first B0 run predates the precision change, which is expected to move latency and batching precision, and it has no per-question results. |
| 39 | SFT details: loss is the mean cross-entropy over every slot in a micro-batch on the fp32 letter logits; each epoch's order and option shuffles come from `random.Random(f"{seed}:epoch{epoch}")`, so a resumed run replays the same data exactly; the best adapter is chosen by NLL on a fixed seeded subset of 1000 valid records, validated every 200 optimizer steps and at the last step, and the final full-valid pass uses that best adapter; a run directory that already holds a training run is never overwritten without `--resume`; run names are `sft_06b` and `sft_17b`, with smoke runs as `sft_<size>_smoke` (gitignored) | Averaging over slots weights every question equally whatever the record's question count; per-epoch seeds make resume equivalent to an uninterrupted run, which the tests check to 1e-6; a fixed subset keeps validation comparable across steps and cheap; refusing to overwrite protects a finished run from a mistyped command; the run names follow the task 1.7 spec and the notebooks. |
| 40 | Data v1.1: every noul question of both kinds, in every split, is stored in its negated phrasing with probability 0.5 (seeded, its own stream) with the gold answer flipped and `meta.negated` recorded; `jevmark/data/negation.py` maps either phrasing to the other, so `negate(negate(q)) == q` and the symmetry evaluation works for records stored in either phrasing; the in-scope out_of_scope rate is set once from train so the kind has about 1750 questions there (p = 0.1154, used in every split, which also gives test_unseen_intents out_of_scope questions); about_domain's asked-domain probability becomes q = (A + N_oos) / (2A) with A = N_in (1 - p); the build reports per-phrasing balance and a phrasing-only accuracy | sft_06b on v1 matched the domain phrase and ignored negation (89 percent of about_domain pairs got the same answer both ways), and out_of_scope had only 498 train questions and 0.75 test accuracy. Negation forces the model to read the question, and a larger out_of_scope kind gives it more signal. Known limitation, measured at build time: out_of_scope's underlying answers are about 1 true to 6 false in train, so after negation the phrasing alone predicts its answer 85.8 percent of the time in train (100 percent in test_unseen_intents, 29.7 percent in test_indomain); negation balances each kind overall but cannot balance each phrasing without more out-of-scope utterances. about_domain has no such shortcut (phrasing-only accuracy 50 percent). |
| 41 | Data v1.2, noul and score diversity: `out_of_scope` asks exactly as many in-scope utterances as there are out-of-scope ones (option 1 of the v1.1 review, so no `out_of_scope` in test_unseen_intents); every CLINC record gets a third question, `about_intent`, asking one description variant of the gold intent in exactly half of the records and of another allowed intent otherwise (held-out intents only in test_unseen_intents); every non-neutral SST-5 record gets `is_positive` or `is_negative`; 30 percent of SST-5 records use a hand-written 3-level scale; test_emotion gets `expresses_emotion`; the new split `test_yelp` (200 reviews per star from `Yelp/yelp_review_full` test, reviews of at most 2500 characters) is the unseen score schema. Every noul kind has two or three templates with paired negations (`negation.py`), and `build.assign_phrasings` gives each question its template and polarity from shuffled balanced blocks within each kind and underlying answer. The build fails if any noul kind in any split has a yes share outside 40 to 60 percent or a phrasing-only accuracy above 55 percent | sft_06b on v1 learned one phrasing per kind and matched keywords; more kinds, templates, negations and a second score scale make the answer depend on reading the question rather than recognising its surface form. v1.1 balanced each kind but let the `out_of_scope` phrasing predict the answer 85.8 percent of the time in train; balancing the underlying answers exactly and assigning phrasings within each answer group removes that shortcut by construction, and the 55 percent bound keeps it removed (measured: at most 52.8 percent, SST-5 `is_positive` in train, where the dataset's labels set the balance). Random negation alone could breach 55 percent in small splits by chance, hence the exact counts. The Yelp length cap keeps 97.4 percent of reviews and avoids truncation. |
| 42 | Data v1.3 and a change of method: data is validated by CPU gates (`make data`: build checks, leak probes, duplicate check) and a 20-minute GPU cycle (the `FAST` mode of `notebooks/kaggle_train.ipynb`) before any full Kaggle session, and v1.3 is frozen for v1 and v2 once it passes both. v1.3: every record has at most one gold-dependent noul, after the choice or score question (CLINC `intent` then one of `about_domain`, `about_intent` or `out_of_scope`; SST-5 `sentiment` then its sentiment noul; emotion `label` then `expresses_emotion`); `out_of_scope` keeps the option 1 rule, so its configured weight of 0.10 is unused and every other CLINC record draws `about_domain` or `about_intent` one half each; a "no" `about_intent` asks a distractor option with probability 0.8 and `about_domain` a distractor option's domain with probability 0.8; K is uniform in 3..14; 0 to 2 label-independent form nouls per record; 20 percent of every split has a JSON state with 1 to 3 random fields plus `text`. Leak gate: two probe models, logistic regression and a histogram gradient-boosted tree ensemble (HistGradientBoostingClassifier, on the 256 most frequent feature columns of each group, one thread), on state-free features (phrasing, question text, structure, cross-question; each computed on the question and the questions before it only), 5-fold cross-validation, the same rule for both; a probe more than 10 points over its group's majority baseline fails unconditionally; one more than 3 points over it fails if its lift is also above the 99th percentile of 200 runs on shuffled targets; the summary lists every probe above 3 points, passing or not, with n, lift and p value. Duplicate gate: any normalised text shared by train or valid and a test split fails | The v1.2 review found order leaks that full sessions had trained on: a sentiment noul before the score told the model the text was not neutral, `about_intent` before the choice primed it, and `about_intent` and `about_domain` in one record were correlated through the gold. Attention is causal, so a question can only be influenced by the ones before it, and one gold-dependent question after the choice or score question removes every such path by construction; for the same reason the probes ignore later questions. Feature matching keeps "is the asked item among the options" from predicting the answer. The option 1 rule is exact balance, which a 0.10 weight cannot give in test_indomain (1000 out-of-scope against 3900 in-scope utterances). JSON states match what the API accepts. With about 250 probe results per build, chance alone lifts a few small groups past 3 points and past a 95th percentile (measured: test_agnews `word_count_over`, +5.5 points on 311 questions, p 0.04 against its shuffled targets, features random by construction); the 99th percentile keeps those from failing the build, every real leak found so far was above 20 points, so the 10-point hard limit needs no test, and listing every lift above 3 points shows a reader the expected false positives instead of hiding them. The tree ensemble covers interaction leaks a linear probe cannot represent (an exclusive or of two features, a flag that matters only for some K); dense input limits it to the most frequent columns, which keeps every phrasing and structure column. Cheap CPU gates and a short GPU cycle catch a broken data version before it costs a full session. |
| 43 | Fixes the v1.3 gates forced before v1.3 passed: (1) train and valid drop every utterance whose normalised text (`dedup.normalise`: NFKC, lower case, non-alphanumeric runs to one space) occurs in a source split a test split draws from (CLINC test and every held-out-intent utterance, SST-5 test, and the test splits of AG News, emotion, Banking77 and Yelp): 18 CLINC and 3 SST-5 records leave train, 5 CLINC records leave valid, test splits are unchanged (the human chose train and valid over test); (2) `expresses_emotion` "no" questions ask each emotion exactly as often as the "yes" questions do (`unseen.matched_asks`), instead of a uniformly drawn other emotion; (3) form nouls are drawn and placed at random, independently of the text and every gold answer: thresholds per split and source on that split's texts, a kind used only where its best split is within 48 to 52 percent, no trimming to exact balance, and each form noul before the first gold-dependent question with probability 1/3, else anywhere after it; (4) the question-text probe is skipped for choice gold position | (1) CLINC150 and SST-5 contain a few identical texts across their own train and test splits (for example "where did you grow up", "no"); dropping them from the larger training side keeps the pinned test sets comparable across versions. (2) The first v1.3 build let the question text predict `expresses_emotion` 18.7 points above chance, because gold emotions are skewed (joy and sadness dominate) while the asked "no" emotion was uniform. (3) Trimming to exact balance selected records by a text property, which tied form-noul presence to text length and so to labels (test_indomain `about_domain` structure probe +3.0 points, above its shuffled null); uniform insertion among existing questions put a form noul before the SST-5 score with probability 1/2 in neutral records and 1/3 in the others, a small label leak. Per-split thresholds balance the answers without selecting records. (4) A bag of words has no position information, and that probe was 124 of the 290 seconds the gate took. |
| 44 | Form nouls only in the splits built from the training sources (train, valid, test_indomain, test_unseen_intents, test_sst5); none in the evaluation-only splits (test_agnews, test_emotion, test_banking77, test_yelp). Headline metrics (`overall` and the per-type blocks, every split) cover gold-dependent questions only; form nouls are reported in a separate `form` block with the same metrics. train, valid, test_indomain, test_unseen_intents and test_sst5 are byte-identical to the previous build; the four evaluation-only files differ only by the removed form nouls | On the long texts of the evaluation-only splits (news articles, reviews), a form noul measures counting words or characters, which is not a decision capability and was never trained: training has form nouls only on short CLINC and SST-5 texts. The v1.3 fast cycle showed it, 0.43 to 0.58 accuracy with ECE up to 0.51 on AG News and Yelp form nouls, which dragged those splits' overall accuracy and ECE down for a reason unrelated to the question the split asks. Form nouls stay in the training-source splits, where they vary question position without leaking a label. test_sst5 is not evaluation-only (it is the in-domain SST-5 test), so it keeps them. |
| 45 | Data v1.3 is frozen for v1 and v2. Run directories in a clone are history, not state: `train_sft.py` refuses a fresh start when the run directory holds `train_summary.json`, `training_log.jsonl`, `adapter/` or `last/`, and the notebooks delete committed training run directories right after the clone. The pre-flight check is mandatory: every training run, every size, first runs one forward and backward pass on its worst-case micro-batch under its own precision settings and stops with PREFLIGHT FAIL on a CUDA out-of-memory error. The notebook training cells pass only on exit code 0 and a `train_summary.json` written in this session with the planned steps, and evaluation runs only after a pass. 0.6B trains at micro-batch 8 with accumulation 4 (effective batch 32), and the notebooks set expandable CUDA segments | The first full 0.6B session on v1.3 ran out of memory at step 392 of 1378 (T4, 14.56 GiB, 12.64 GiB allocated): v1.3 records are longer (up to 392 tokens, up to four questions) than v1.2's, and micro-batch 16 without gradient checkpointing no longer fit. A failing `!python` does not stop a Kaggle cell, and the old completion check only tested that `train_summary.json` existed, so the v1.2 summary committed to git passed it and the step-200 adapter was evaluated as `sft_06b`. The pre-flight check turns an out-of-memory error an hour into a session into one in the first minute; the guard and the in-session check make a stale file impossible to mistake for a result. `runs/base_06b` from that session is valid (it does not depend on training) and is committed; `runs/sft_06b` is not. |
| 46 | Data v1.3 is frozen for v1 and v2, and the 0.6B result stands: `runs/sft_06b` and `runs/base_06b` at commit a1dc2bf. Every evaluation keeps the full protocol, all nine splits in full with train included, so runs stay comparable | The v1.3 0.6B session completed (1378 of 1378 steps, all gates and checks passed), and the review found nothing that needs a data change: phrasing-only baselines at 50 percent, the v1.2 priming signature gone on the full splits, and the SST-5 neutral shift absent after full training. The frozen-base evaluation reproduced exactly across two sessions, so a later run differs from this one only by what it changes. A run on a sample or a subset of splits could not be compared with these. |
| 47 | Baselines (task 1.8): one fixed subset, `data/baseline_subset.json`, 500 records per split for all nine splits, drawn with the fast-cycle sampler and the `--limit` seed (`random.Random(f"limit:{split}")`, so it is what `evaluate.py --limit 500` evaluates) plus a 200-record sub-subset drawn from it (`baseline-sub:{split}`), stored with the sha256 of each data file and committed through a `.gitignore` exception (`/data/*` then `!/data/baseline_subset.json`). One prompt and one parser for B1 and B2: every question as an anonymous id (q1, q2, ... in request order, also the keys of the JSON schema, mapped back to the real ids after parsing), type, instructions and options (noul descriptions default to yes and no, as in the encoding), one JSON object back; the parser strips surrounding whitespace and at most one Markdown code fence, accepts a bare answer without confidence, drops a confidence outside [0, 1] but keeps the answer, and records `invalid_json`, `missing` or `invalid_answer` apart from wrong answers. Metrics keep the evaluate.py layout; `accuracy` is on parsed answers and `accuracy_all` counts failures as wrong and is the number compared with jevmark; ECE and coverage use the verbalized confidence of parsed answers that state one. B1 is Qwen/Qwen3-1.7B at revision 70d244cc, greedy, fp16 with the first-batch NaN check and fp32 reload, prompts sorted longest first within a split; its latency requests are the first 200 of train.jsonl, the ones evaluate.py times. B2 uses a strict JSON schema per request (every answer an enum or boolean, confidence required because strict mode requires every field), one call at a time, and a cap checked before each request against the spend so far plus a worst case (prompt characters / 2 input tokens, every allowed output token). Replies go to `runs/<run>/replies.jsonl`, gitignored like `results.jsonl.gz`, and every number, ours included, is recomputed from per-question evidence on the subset by `compare_baselines.py`. `sample_records` moved to `jevmark/sampling.py` and `git_state` to `jevmark/provenance.py`, so scripts share them without importing each other | One subset for every run makes the table a paired comparison; reusing the `--limit` sample means a jevmark run could also be evaluated on exactly these records directly. Committing ids with data hashes pins the subset to the frozen v1.3 build (decision 46) and fails loudly on any other. Parse failures are a cost of generating instead of reading out, so they are measured, not hidden in accuracy; lenient formatting (a fence, a string "true") is not what the comparison is about, while an answer outside the options is. A strict schema is how an API model is used in practice, so B2 rarely fails to parse and measures accuracy and cost; B1 without constrained decoding measures what a small generator does unaided. A worst-case check before each call means the cap cannot be crossed by the call that finds it. Real question ids such as `is_positive`, `about_domain` or `expresses_emotion` name the question kind, which jevmark never sees (API_SPEC section 4), so showing them would give the baselines information the model does not have. B1 runs at both sizes, Qwen/Qwen3-0.6B (revision c1899de2) and Qwen/Qwen3-1.7B (revision 70d244cc), so each jevmark size has a same-size generate-JSON counterpart. B2 runs at temperature 0 and records per reply the snapshot the API reports, the request time and the prices used, because an API model name moves to new snapshots and prices change; B2's schema makes parse failures near zero by construction, so format reliability is read from B1. |
