# jevmark

A small System One decision model: text state and typed questions in, probability distributions and confidence out, in one forward pass, with no text generation. An independent re-implementation of the concept behind TypeSafe AI's Jev, trained with LoRA on Qwen3-1.7B-Base (Qwen3-0.6B-Base for fast iteration) and, in phase 2, with Reinforcement Learning for Calibrated Decisions.

Start with `docs/PLAN.md`. The API contract is `docs/API_SPEC.md`. The ordered task list is `docs/TASKS.md`. Rules for working in this repo are in `CLAUDE.md`.

Status: planning complete, no code yet.
